server:
  port: 8060

spring:
  application:
    name: nacos-provider

  cloud:
    nacos:
      config:
        server-addr: localhost:8868
        file-extension: yaml
      discovery:
        server-addr: localhost:8868

